<div class="divisor"></div>

<style>
    .divisor {
        display: block;
        height: 48px;
        width: 2px;
        background: linear-gradient(
            to bottom,
            var(--accent),
            var(--secondary) 65%
        );
        box-shadow: rgba(51, 51, 51, 0.35) 0px 6px 6px;
        border-radius: var(--radius);
        margin: var(--space-2xl) auto;
    }
</style>
